.ruby-lsp
vendor
.gem_rbs_collection

# The following should be removed once the project is cloned, as they are generated by the project and should not be
# committed to version control. At the template these are ignored to address with different version of Ruby and gems
# used by different cases of the template.
Gemfile.lock
rbs_collection.lock.yaml
